<div class="timer">{{timer}}</div>

{{#if running}}
  <button type="button" {{action "stop"}}>Stop</button>
{{else}}
  {{input type="text" value=minutes placeholder="Minutes"}}
  <button type="button" {{action "start"}}>Start</button>
{{/if}}

{{outlet "people"}}
